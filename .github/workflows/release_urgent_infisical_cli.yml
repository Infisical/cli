name: Build and release CLI (Urgent)

on:
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  release:
    uses: ./.github/workflows/release_build_infisical_cli.yml
    with:
      is_urgent: true
    secrets: inherit
